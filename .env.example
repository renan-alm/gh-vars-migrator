# gh-vars-migrator environment configuration
#
# Copy this file to .env and fill in the values for your migration.
# CLI flags always take precedence over values defined here.
# Environment variables already set in your shell override .env values.
#
# Priority: CLI flag > shell env var > .env file

# ── Source ────────────────────────────────────────────────────────────
SOURCE_ORG=
SOURCE_REPO=
SOURCE_PAT=
SOURCE_HOSTNAME=

# ── Target ────────────────────────────────────────────────────────────
TARGET_ORG=
TARGET_REPO=
TARGET_PAT=
TARGET_HOSTNAME=

# ── Shared token (used for both source and target when PATs are not set)
# GITHUB_TOKEN=

# ── Mode (set to true to enable) ─────────────────────────────────────
# ORG_TO_ORG=false
# SKIP_ENVS=false

# ── Behaviour ─────────────────────────────────────────────────────────
# DRY_RUN=false
# FORCE=false
